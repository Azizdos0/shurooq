// Shared, validated vocabulary for the editor + API.

export const SECTION_IDS = ['progress', 'lost', 'people', 'without', 'internet'];

export const PIECE_STATUSES = ['draft', 'ready'];

// The three editorial verification tiers (mirrors the About page).
export const TAGS = ['verified', 'reported', 'reconstructed'];

export const TAG_LABELS = {
  verified: 'موثَّق',
  reported: 'روايةٌ متواترة',
  reconstructed: 'إعادة تخيُّل',
};
